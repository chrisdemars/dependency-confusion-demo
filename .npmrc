# Map our specific scope to the local Verdaccio registry
@my-org:registry=http://localhost:4873

# Optional: To demo the 'unscoped' attack, 
# you can temporarily set the global registry here:
# registry=http://localhost:4873